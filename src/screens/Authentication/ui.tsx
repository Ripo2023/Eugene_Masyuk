import React from "react";
import styled from "styled-components/native"

import { WHITE_COLOR } from "../../shared/config"

export const AuthenticationScreen: React.FC = () => {
    return (
        <Container>

        </Container>
    )
}

const Container = styled.View`
    flex: 1;
    background: ${WHITE_COLOR}
`
